# Deprecated Iterative Ideation Reference

This reference is deprecated. Evaluator feedback for hypothesis generation now lives in `references/mechanistic_hypothesis_generation.md` under evaluator-grounded refinement.

Use the new protocol after a minimal test or evaluator result arrives. Treat the result as a new observation, state which hypothesis type is being revised, and update the typed hypothesis record with `commit / park / kill`. For mechanistic hypotheses, state which mechanism explanation was ruled out and keep live competing explanations explicit.

This stub remains only so older links fail clearly instead of silently invoking the former evaluator-ranked candidate workflow.
