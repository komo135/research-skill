# Charter — <REPLACE: project name, kebab-case>

Status: DRAFT (change to READY after review)

---

## H1. What capability should exist?

<REPLACE: one sentence, no jargon, no internal library names. Imagine
explaining to a smart person from a different domain. See
`references/rd/rd_charter.md` § H1 for examples.>

## H2. How is this approximated today, and what are the limits?

<REPLACE: 1-3 sentences. Describe the current best practice (the team's
or the field's) and what is unsatisfactory. Quantify the limit if
possible. See § H2 for examples.>

## H3. What is new in the proposed approach, and why might it work?

<REPLACE: 1-3 sentences. Name the specific novelty (algorithm class,
data source, decomposition, validation method). State the prior evidence
or mechanism that makes success plausible. Capture novelty axis here
(existing / extension / novel) — do not duplicate on individual core
technologies. See § H3 for examples.>

## H4. Who cares? Who is the consumer, and what decision does this capability serve?

<REPLACE: name a specific consumer (a person, a system, a workflow) and
the decision currently blocked. If you cannot name the consumer and the
blocked decision, the capability has no demand. See § H4.>

## H4b. Research payload boundary

<REPLACE: state what this Capability / Technology Research workstream will and
will not claim. If this workstream builds a framework, pipeline, evaluator, or
infrastructure, its success is the capability working against fixed acceptance
fixtures. Newly discovered effect, factor validity, market mechanism, or
strategy profitability claims must be tracked as separate project-instance
trials, and promoted only through the appropriate capability gate or a
Phenomenon / Mechanism Research workstream.>

## H5. If we succeed, what changes downstream?

<REPLACE: describe the world after success in concrete terms. What
workflows change? What metrics improve? What gets retired? Quantify if
possible. See § H5.>

## H6. What are the dominant risks, and what evidence would kill this workstream?

<REPLACE: list 3-5 risks. For each, name **specific observable evidence**
that would kill the workstream. These evidence statements become the
workstream's binding kill criteria, stated here. See § H6 for examples.>

1. **<risk name>**. KILL if <numeric or behavioral threshold + condition>.
2. **<risk name>**. KILL if <...>.
3. **<risk name>**. KILL if <...>.

## H7. Cost: budget for compute, data, and wall-clock

**One-time** (charter, decomposition, initial setup, initial
training/fine-tuning):
- Compute: <REPLACE>
- Data: <REPLACE>
- Wall-clock: <REPLACE>

**Recurring** (re-tuning cadence, monitoring, ongoing data feed; only
applicable if any core tech will be `continuous-improvement` — leave blank if all
establish-once):
- Compute (per cycle): <REPLACE or N/A>
- Data (per year): <REPLACE or N/A>
- Wall-clock (per cycle): <REPLACE or N/A>

## H8. Midterm exam, final exam, AND integration pattern

**Midterm exam** (concrete demonstration that the hardest sub-question
has been answered, typically TRL-3 of the highest-risk capability):

<REPLACE: what specifically demonstrates this. Name the metric, the
threshold, the data, the duration.>

**Final exam** (TRL-6 operational prototype demonstration on
representative workload, all core technologies `established`, plus —
if applicable — maintenance plan filed):

<REPLACE: what specifically demonstrates this. Lifecycle-aware: if any
core tech is expected to be `continuous-improvement`, the final exam includes the
maintenance plan being scheduled, not just the v1 demo.>

**Integration pattern** (REQUIRED, see
`references/rd/integration_patterns.md`):

Pattern: <REPLACE: Pattern 1 (vertical slice / framework-first) | Pattern 2 (bottom-up / component-first) | Pattern 3 (skeleton + spike, recommended default)>

Reason: <REPLACE: architecture certainty + K interface stability + stakeholder
confidence considerations. See decision tree in integration_patterns.md.>

If Pattern 1 or 3: list the baseline / skeleton components you will build
upfront before any K Stage-Gate cycle:

- <REPLACE: component 1 + naive implementation>
- <REPLACE: component 2 + naive implementation>
- <REPLACE: ...>

---

- Drafted by: <REPLACE: agent name and/or user>
- Reviewed by: <REPLACE: user>

---

After this charter is reviewed, proceed to `references/rd/core_technologies.md`
to define Layer 1 (intellectual decomposition) before any capability
writing or implementation.
