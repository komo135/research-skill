"""purpose.py.template (RETIRED)

Replaced by workstream-targeted trial notebook templates:

- R&D Workstream: `assets/rd/rd_trial.py.template` (evidence artifact)
- Phenomenon / Mechanism Research: `assets/pure_research/pr_trial.py.template`
  (evidence artifact)

Use `scripts/new_trial.py --project-dir <path> --workstream <name> --slug
<name>` — the script selects the template from the workstream state object.

Do not use this template for new trials.
"""
