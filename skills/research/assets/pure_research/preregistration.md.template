# Pre-Registration — PR_<REPLACE: id, e.g., 001>

Status: DRAFT (change to READY after review, before trial execution)

PR/FAQ: <REPLACE: path to prfaq.md section that scopes this trial>

---

## 1. Question

<REPLACE: one sentence. Identical to or a sub-question of the PR/FAQ
question. Frame as an unknown to reduce.>

## 1.1 Confirmation target and initial approach

- **confirmation target**: <REPLACE: what this PR is trying to confirm or discriminate:
  question, E-pair/null, scope, primary metric, threshold, and interpretation
  rule>
- **initial approach**: <REPLACE: planned analysis method, estimator,
  data-acquisition route, implementation path, period-fetch mechanics, and
  operational choices>

The initial approach is not the confirmation target itself. An
initial-approach change that preserves the confirmation target, threshold,
scope, and interpretation is not a major deviation.
If evidence, data access, or implementation constraints require an approach
change while the confirmation target remains fixed, document the rationale and
continue under this PR; a new PR is not required and do not treat the change
as hypothesis failure.

## 2. Competing explanations (≥2 required, including null/artifact)

For each E, also declare evidence type (causal / correlative /
null-result) and (if correlative) the gap to causation. See
`references/pure_research/preregistration.md` § 2.

| ID | Statement | Mechanism | Ex ante predicted observation | Evidence type | Causation gap (if correlative) |
|---|---|---|---|---|---|
| E1 | <REPLACE: explanation statement> | <REPLACE: causal mechanism> | <REPLACE: what observation would best support this E> | causal \| correlative \| null-result | <REPLACE or N/A> |
| E2 | <REPLACE> | <REPLACE> | <REPLACE> | <REPLACE> | <REPLACE or N/A> |
| E_null | <REPLACE: null / artifact / measurement-error / sample-variance explanation> | <REPLACE> | <REPLACE: e.g., bootstrap CI includes 0> | null-result | N/A |

## 3. Test design

### 3.1 Data

- **Source**: <REPLACE>
- **Period**: <REPLACE: start to end>
- **Frequency**: <REPLACE: e.g., daily, 5-min>
- **Universe**: <REPLACE: instruments included>
- **Planned data identity**: <REPLACE: path / vendor version / expected
  snapshot date. Fill the actual data version in the trial note.>

### 3.2 Sample / split

<REPLACE: how data is split for the test. Holdout windows? repeated validation?
Train / val / test? Sample size per split?>

### 3.3 Metric

- **Primary**: <REPLACE: single number, e.g., difference in rolling
  3-year primary metric across two periods>
- **Secondary**: <REPLACE: list any secondary metrics that will be
  reported>

### 3.4 Threshold

- **Support E1 if**: <REPLACE: observable threshold>
- **Support E2 if**: <REPLACE: observable threshold>
- **Support E_null if**: <REPLACE: observable threshold>
- **Ambiguous if**: <REPLACE: condition where no E clearly wins>

Threshold miss is result interpretation, not deviation. If the observed result
misses a pre-registered threshold, interpret it under the rules above; do not
change the threshold after seeing data and do not open a new PR solely because
the threshold was missed.

### 3.5 Multiple testing correction

- **Number of distinct hypotheses tested across this trial**: <REPLACE>
- **Number of distinct hypotheses tested across the whole project so
  far**: <REPLACE: be honest>
- **Correction method**: <REPLACE: Bonferroni / Romano-Wolf step-down
  / domain-appropriate selection correction or significance hurdle>
- **Corrected significance threshold**: <REPLACE>

## 4. Expected diff under each explanation

(Forces ex ante prediction. After the trial, deviation review compares
actual results against these predictions.)

- **If E1 true → expected pattern**: <REPLACE: precise, observable>
- **If E2 true → expected pattern**: <REPLACE>
- **If E_null true → expected pattern**: <REPLACE>

## 5. Stop condition

<REPLACE: the observable condition that ends this trial. Without
this, trials drift.>

## 6. Reproducibility

- Random seed(s): <REPLACE>
- Code commit (planned at trial run): <fill at trial time>
- Environment pin (planned at trial run): <fill at trial time>
- Tracking note/backend (planned): <run note, results row, or selected tracker>
- Decision-relevant run set (planned): <only if selection, multiple-testing correction, or promotion will depend on multiple attempts>
- Data version (final, trial-time): <fill from run note or selected tracker>

---

- Drafted by: <REPLACE: agent and/or user>
- Reviewed by: <REPLACE: user>

---

After this pre-registration is reviewed, the trial may run. Post-trial,
compare the trial against `PR_<id>` and apply the deviation severity matrix in
`references/pure_research/pr_workflow.md`.
