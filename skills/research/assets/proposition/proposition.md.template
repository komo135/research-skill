# <proposition_id>: <Proposition title>

## Proposition

<proposition_statement>

## Current status

open | supported | contradicted | unrealized-condition | under-specified | split-needed | split | closed

## Why it matters

<What becomes possible to explain, predict, design, or rule out if this proposition is clarified.>

## Current expected consequence

<expected_consequence>

## Live working propositions

- None yet. Add `WPxxx` pointers to `analyses.md` when an analysis produces a working proposition that remains live across sessions.

## Live derived hypotheses

- None yet. Add `Hxxx` pointers after `new_hypothesis.py` creates derived hypotheses.

## Key material

- Observation: <pointer to `observations.md`, or material still missing>
- Analysis: <pointer to `analyses.md`, or analysis still missing>

## Update rules

- Support if: <observed material matches the current expected consequence under stated conditions>
- Contradict if: <observed material breaks the proposition itself, not merely an implementation condition>
- Mark unrealized-condition if: <the proposition may hold, but a required condition, representation, search path, measurement, or evaluator is not realized>
- Mark under-specified if: <the proposition cannot yet produce a discriminating expected consequence>
- Mark split-needed if: <one proposition hides multiple separable propositions>
- Split if: <child propositions are opened and the parent is no longer the active route>
- Close if: <the proposition is resolved, superseded, killed, or no longer useful>
